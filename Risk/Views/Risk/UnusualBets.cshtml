@model IEnumerable<Risk.Data.UnSettled>

@{
    ViewBag.Title = "High Bets";
}
<h2>@ViewBag.Title.</h2>
<h3>@ViewBag.Message</h3>

<table>
    <tr>
        <th>Customer</th>
        <th>Event</th>
    </tr>
    @foreach (var item in Model)
    {
        <tr>
            <td>@Html.DisplayFor(model => item.Customer)</td>
            <td>@Html.DisplayFor(model => item.Event)</td>
        </tr>
    }
</table>